# Plum-Audio backend — Python 3.13
# Sync engine: pin aiosendspin (fast-moving). On bump, run _resources/spike/mesh_smoke.py first.
aiosendspin==6.0.5
av>=18,<19            # PyAV — audio encode/resample (glibc wheels on Debian base)
numpy>=2,<3
Pillow>=10            # REQUIRED by aiosendspin's artwork role (server won't import without it)
sounddevice>=0.5      # PortAudio player output (needs libportaudio2 in the image)

# REST APIs
Flask>=3,<4
flask-cors>=4
requests>=2.31

# MPRIS control/metadata over the system D-Bus: AirPlay (shairport-sync) and Spotify (spotifyd).
# asyncio-native (no glib main loop) so it lives in the audio event loop. Needs a system dbus-daemon
# + the run user allowed to own/send the MPRIS names (config/shairport-sync-dbus.conf, spotifyd-dbus.conf).
dbus-next>=0.2.3,<0.3

# Utility
aiohttp>=3.9          # transitive via aiosendspin; pinned for clarity
